#!/usr/bin/env bash
# TODO: Detach process?
prefix=".deleting.$(hostname).$$"
for src in $@; do
	src="${src%/}"
	dest="$(dirname $src)/$prefix.$(basename $src)"
	mv "$src" "$dest"
	echo "rm -rf $src"
	rm -rf $dest &
done
